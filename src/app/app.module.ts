import { NgModule } from '@angular/core';
import { component, imports, bootstrap, providers } from './declarations';

@NgModule({
  declarations: component,
  imports: imports,
  providers: providers,
  bootstrap: bootstrap,
})
export class AppModule {}
